"Decidi seguir com uma base sólida em TypeScript puro para as regras de negócio (Domínio) e usarei o NestJS como a armadura de segurança e infraestrutura. O custo será zero no início usando camadas gratuitas, e a segurança será reforçada pelos padrões que o Nest exige."

Também usarei o Prisma para infra.


Data: 2026-04-08
Decisão: Transformar "Gênero" em uma Entidade, não em um Enum.
Motivo: Necessidade de rastreabilidade (saber quando o termo foi incluído) e padronização para análises futuras (Dashboards).
Técnica Utilizada: Implementação de um slug no domínio para evitar que variações de digitação (ex: Pansexual vs pansexual) gerem duplicidade de dados, facilitando a contagem e agrupamento no Superset/Metabase.


Sobre IDs: "Decidi gerar IDs (UUID) na camada de Domínio usando randomUUID. Isso garante que a Entidade tenha identidade própria antes de tocar o banco de dados, seguindo os princípios do DDD."

Sobre Slugs: "O Slug será gerado automaticamente a partir do título do Gênero. Ele servirá como nossa chave de busca padronizada, evitando que variações de acentuação ou letras maiúsculas/minúsculas poluam nossas análises estatísticas futuras."


Refatoração de Getters e DTOs:
"Optei por não retornar this.props diretamente para evitar vazamento de referências e garantir o encapsulamento. Implementei getters individuais e o método toDTO() (ou Mapper) para preparar os dados para o Frontend. Isso garante que o React receba apenas o necessário, mantendo as regras de negócio protegidas dentro da Entidade."


Implementação de Mappers/DTOs: "Decidi separar a representação interna da entidade da sua saída externa. Usei interfaces de DTO para definir contratos rígidos com o Frontend. Isso protege a evolução do banco de dados sem quebrar a interface do usuário."


Organização de Contratos (DTOs): "Decidi separar as interfaces de DTO em arquivos próprios. Isso permite que esses contratos sejam compartilhados ou espelhados no Frontend (Next.js), garantindo que a comunicação entre API e UI seja 100% tipada e segura, evitando erros de 'propriedade indefinida' em produção."

Tecnologias de Ecossistema:
"Conheci o conceito de Monorepos com Nx. Ele facilita a vida ao permitir que Frontend e Backend compartilhem as mesmas interfaces (DTOs), garantindo que os dois 'falem a mesma língua' sem precisar copiar e colar código."


Tipo vs. Valor:
"Entendi que não posso retornar uma Interface (GenderDTO) diretamente. Interfaces são apenas 'moldes' que somem após a compilação. Para enviar dados, preciso criar um objeto real {} que preencha os requisitos desse molde. Isso garante que o Backend entregue dados concretos e validados, e não apenas definições abstratas."

Uso de Enums para Pronomes:
"Decidi usar Enums para pronomes por ser uma lista mais estável. No DTO, exporto o tipo do próprio Enum para que o Frontend tenha preenchimento automático (IntelliSense) e não permita o envio de pronomes inválidos, mantendo a padronização dos dados desde a entrada."

Arquitetura de Tipos:
"Decidi centralizar Enums em src/domain/enums. Isso facilita a exportação desses valores para o Frontend e garante que definições globais (como Pronomes) fiquem em um local único, evitando duplicação de código e facilitando a manutenção caso novos pronomes precisem ser adicionados ao sistema base."

Divisão de Responsabilidades (Entidade vs. Use Case):
"Entendi que a Entidade é responsável pela validação de formato e regras intrínsecas (ex: tamanho de sigla, valores de Enum). O Use Case é responsável pelas regras de negócio que dependem de fatores externos (ex: unicidade de CPF no banco de dados, verificação de siglas duplicadas em diferentes tabelas). Essa separação facilita o teste do código e a manutenção futura."